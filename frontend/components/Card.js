import styled from '@emotion/styled';

export const Button = styled.button`
  background: #0a2540;
  color: white;
  padding: 8px 16px;
`;

export const Card = styled.div`
  border: 1px solid #ddd;
  padding: 16px;
`;
// No server-side style extraction / SSR config configured (no @emotion/server, no SSR setup)
